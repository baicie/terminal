/**
 * TerminalContainer - 兼容层
 *
 * 此组件已重构为使用 features/terminal/components/ 中的独立模块。
 * 保留此文件以保持向后兼容，新代码应直接使用新组件。
 *
 * @deprecated 请使用 @/features/terminal/components
 */

export { TerminalContainer } from '@/features/terminal/components/terminal-container/container'
import { TerminalContainer } from '@/features/terminal/components/terminal-container/container'
export default TerminalContainer
