# pnpm workspace configuration
auto-install-peers=true
enable-pre-post-scripts=true

# Hoist all deps to root node_modules (useful for monorepo tooling)
# public-hoist-pattern[]=*eslint*
# public-hoist-pattern[]=*prettier*
